# Dépendances (volumineuses, recréées via npm install)
node_modules/

# Build (recréé via npm run build)
dist/

# Runtime local Opportunity Radar
data/
*.sqlite
*.sqlite-shm
*.sqlite-wal
*.db
*.db-shm
*.db-wal
logs/

# Variables d'environnement — NE JAMAIS committer
.env
.env.local
.env.*.local
candicator_nocommit.env

# Fichiers JSON privés exportés de consoles API — NE JAMAIS committer
*.json
!package.json
!package-lock.json
firebase*clé*
firebase clé privée no commit

# Documentation locale uniquement
SETUP.txt

# Système macOS
.DS_Store
